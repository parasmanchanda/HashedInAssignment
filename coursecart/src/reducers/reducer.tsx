import { initialState, LoginState} from "../components/Course/CourseDetails/CourseDetails";

type LginState = typeof initialState

export const reducer = (state: Array<LoginState>, action : any) => {
    return state;
};